# classifier_ResNet_pld
app/                        # Thư mục chứa toàn bộ source code
├── api.py                  # file api fastapi 
└── app.py                  # file streamlit chứ giao diện code
└── model_pld.pth           # model phân loại để sử dụng
